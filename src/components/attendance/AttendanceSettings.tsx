// AttendanceSettings component - placeholder for now
import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

const AttendanceSettings: React.FC = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>إعدادات الحضور</CardTitle>
      </CardHeader>
      <CardContent>
        <p>سيتم تطوير صفحة الإعدادات قريباً</p>
      </CardContent>
    </Card>
  );
};

export default AttendanceSettings;